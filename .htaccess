# Bắt đầu cấu hình Laravel redirect vào /public
RewriteEngine On

# Nếu truy cập thẳng vào file hoặc thư mục thật thì cho phép
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Còn lại thì chuyển hướng toàn bộ về thư mục /public
RewriteRule ^(.*)$ public/$1 [L]
