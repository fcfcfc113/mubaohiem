Options -Indexes
RewriteEngine On
RewriteBase /

# Nếu là request tới file hoặc thư mục thật thì bỏ qua
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Còn lại thì chuyển hướng vào thư mục public
RewriteRule ^(.*)$ public/$1 [L]
